# State Artifact Registry

Every `.claude/state/plan-w-team-*` file has an entry here. This registry is the authoritative list consumed by `.claude/scripts/plan-w-team-symmetry-check.sh` (invoked in Step 5 review).

## Purpose

Prevent the "write-only by accident" defect class: artifacts whose writer is wired but whose reader was promised in prose and never implemented. A registry entry declares intent; the checker verifies the intent matches the code.

## Modes

| Mode          | Fail condition                             | Step 5 behavior                                    |
| ------------- | ------------------------------------------ | -------------------------------------------------- |
| `enforcing`   | Registry entry has no matching reader      | Fail-closed (exit 1, blocks ship)                  |
| `handoff`     | Registry entry has no matching reader      | Fail-closed (exit 1) — handoff with no consumer    |
| `audit-trail` | Reader optional; reported but non-blocking | Advisory warning only                              |
| _(any)_       | Writer grep has no registry entry          | ASK item — either register it or remove the writer |

## Registry

<!-- Format: one row per artifact. Columns parsed by symmetry-check.sh.
     - `pattern`: path with `$SLUG` placeholder or literal for global files
     - `writer_grep`: ripgrep pattern scoped to a code-block match (excluding prose mentions)
     - `reader_grep`: ripgrep pattern matching at least one reader location
     - `mode`: enforcing | handoff | audit-trail
     Do NOT add prose columns before/after without updating the checker's awk. -->

| pattern | writer_grep | reader_grep | mode | purpose |
| -------------------------------------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `.claude/state/plan-w-team-untracked-baseline-$SLUG.txt` | `plan-w-team-untracked-baseline-.*\.txt` | `BASELINE=".claude/state/plan-w-team-untracked-baseline` | enforcing | Ship gate anchor (Step 5 hygiene) |
| `.claude/state/plan-w-team-ac-snapshot-$SLUG.md` | `SNAPSHOT=".claude/state/plan-w-team-ac-snapshot` | `SNAPSHOT=".claude/state/plan-w-team-ac-snapshot` | enforcing | AC contract integrity (evaluator + Step 5) |
| `.claude/state/plan-w-team-scope-lock-$SLUG.json` | `cat > ".claude/state/plan-w-team-scope-lock` | `LOCK=".claude/state/plan-w-team-scope-lock` | enforcing | Scope drift detection (Step 5 + Step 8) |
| `.claude/state/plan-w-team-scope-unlock-$SLUG` | `plan-w-team-scope-unlock-` | `UNLOCK=".claude/state/plan-w-team-scope-unlock` | handoff | User ack for mid-flight scope expansion |
| `.claude/state/plan-w-team-retro-$SLUG.json` | `plan-w-team-retro-\$SLUG\.json` | `RETRO_STATE=".claude/state/plan-w-team-retro` | handoff | Cross-stage hygiene handoff (Step 5 → Step 8) |
| `.claude/state/plan-w-team-friction-log.jsonl` | `LOG=".claude/state/plan-w-team-friction-log` | `plan-w-team-friction-log\.jsonl` | enforcing | Global feedback loop (3-in-30d detector) |
| `.claude/state/plan-w-team-friction-ack-<category>` | `plan-w-team-friction-ack-` | `plan-w-team-friction-ack-` | handoff | User dismissal of friction pattern |
| `.claude/state/plan-w-team-autofix-$SLUG.md` | `plan-w-team-autofix-\$SLUG\.md` | `plan-w-team-autofix-\$SLUG\.md` | handoff | Auto-fix scope fence (reviewer → builder) |
| `.claude/state/plan-w-team-review-findings-$SLUG.md` | `FINDINGS=".claude/state/plan-w-team-review-findings` | `FINDINGS=".claude/state/plan-w-team-review-findings` | enforcing | Review findings handoff (Step 5 → Step 6) |
| `.claude/state/plan-w-team-ack-$SLUG` | `ACK_FILE=".claude/state/plan-w-team-ack` | `ACK_FILE=".claude/state/plan-w-team-ack` | enforcing | Push confirmation gate |
| `.claude/state/plan-w-team-push.lock` | `PUSH_LOCK_DIR=".claude/state/plan-w-team-push\.lock` | `PUSH_LOCK_DIR=".claude/state/plan-w-team-push\.lock` | enforcing | Concurrent push serialization (mkdir lock) |
| `.claude/state/plan-w-team-secret-scan-allow-$SLUG` | `ALLOW_FILE=".claude/state/plan-w-team-secret-scan-allow` | `ALLOW_FILE=".claude/state/plan-w-team-secret-scan-allow` | handoff | User-curated allowlist for secret-scan false positives |
| `.claude/state/plan-w-team-workflow-$SLUG.lock` | `WORKFLOW_LOCK_DIR=".claude/state/plan-w-team-workflow` | `WORKFLOW_LOCK_DIR=".claude/state/plan-w-team-workflow` | enforcing | Per-SLUG concurrent /plan-w-team session lock |
| `.claude/state/plan-w-team-postship-$SLUG.json` | `ARTIFACT=".claude/state/plan-w-team-postship` | `plan-w-team-postship-\$SLUG\.json` | handoff | Step 7 docs audit handoff to Step 8 retro §8d |
| `.claude/state/plan-w-team-friction-log.lock` | `LOCK_DIR=".claude/state/plan-w-team-friction-log\.lock` | `LOCK_DIR=".claude/state/plan-w-team-friction-log\.lock` | enforcing | Concurrent retro friction-log mkdir lock |
| `.claude/state/plan-w-team-coupling-$SLUG.json` | `plan-w-team-coupling-\$\{slug\}\.json` | `COUPLING_REPORT=` | handoff | Stage 2 import-coupling matrix (T2 reads, scope-lock gates) |
| `.claude/state/plan-w-team-coupling-ack-$SLUG` | `> "?\.claude/state/plan-w-team-coupling-ack` | `COUPLING_ACK=` | handoff | User ack for intentional coupling (escape hatch for scope-lock) |
| `.claude/state/plan-w-team-fleet-$SLUG.jsonl` | `FLEET_FILE="\$STATE_DIR/plan-w-team-fleet-` | `FLEET_FILE="\$STATE_DIR/plan-w-team-fleet-` | handoff | Subagent spawn/complete events (T3-04 retro reads, lead reads via fleet-query.sh) |
| `.claude/state/plan-w-team-supervisor-actions-$SLUG.jsonl` | `ACTIONS_LOG="\$STATE_DIR/plan-w-team-supervisor-actions-` | `SUP_LOG=".claude/state/plan-w-team-supervisor-actions-` | handoff | Persistent supervisor (PWT-T4) action audit log (07-retro §8j-quater reads) |
| `.claude/state/plan-w-team-goal-$SLUG.json` | `plan-w-team-goal-\$\{SLUG\}\.json` | `plan-w-team-goal-\*\.json` | handoff | T5b goal evaluator state (slug, started_at, terminal_state, terminal_reason, feature_specific_done_criteria, worker_sid (string, current bg worker session UUID), respawn_history (array of `{ts, old_sid, new_sid, reason, steer_verified}` — `reason` is `steer`, or `steer-refused-ambiguous` with `new_sid:""` when multiple marker-less candidate transcripts made adoption unsafe (task #41); appended by `pwt-steer.sh` — see `shared/supervisor-protocol.md` §Steering a Running Worker)). `worker_sid`/`respawn_history` are per-run: `__pwt_emit_goal_state` re-seeds this file wholesale at spawn, so `respawn_history` never survives across separate `/plan-w-team` runs — only steers within the SAME run accumulate rows. |
| `.claude/state/plan-w-team-spawned-children-$SLUG.jsonl` | `plan-w-team-spawned-children-` | `plan-w-team-spawned-children-` | handoff | `claude --bg` child registry. Readers: (1) 07-retro §8j-sexies cleanup; (2) goal-evaluator hook parent-child terminal propagation. `PLAN_W_TEAM_DISABLE_CHILD_CLEANUP=1` opts out of cleanup only. **Row shapes:** legacy/worker rows have no `type` field (default = worker, `claude stop` on cleanup); `--supervisor-goal` invocations also append a `{"type":"supervisor_mirror","path":"<absolute mirror file path>","session_id":"<worker_sid>",...}` row — readers patch the mirror file at `path` (cleanup → terminal_state=SUCCESS on worker retro; evaluator → terminal_state=LOW_CONFIDENCE_STREAK on dead worker). See `docs/specs/supervisor-mirror-lifecycle.md`. |
| `.claude/state/plan-w-team-orchestrator-decisions-$SLUG.jsonl` | `DECISION_LOG="\$STATE_DIR/plan-w-team-orchestrator-decisions-` | `RETRO_DECISIONS=".claude/state/plan-w-team-orchestrator-decisions-` | handoff | Orchestrator-route decision audit log (PWT-T1b). Writer: `plan-w-team-orchestrator-route.sh`. Reader: 07-retro decision-quality scoring. |
| `.claude/state/plan-w-team-hook-spawn-<parent_sid_short>.flag` | `FLAG_FILE="\$FLAG_DIR/plan-w-team-hook-spawn-` | `plan-w-team-hook-spawn-\*\.flag` | handoff | PWT-DS1 deterministic double-spawn guard. Writer: `plan-w-team-route-prompt.sh` after successful worker spawn. Reader: `pwt-goal.sh` (`--worker-only` and `--launch`) — refuses spawn (exit 3, PWT_DS1_DUPLICATE) if a flag is fresh within `PWT_DOUBLE_SPAWN_WINDOW_MIN` (Tier A, mtime, default 3 min) OR its recorded `worker_sid` is still live in `claude agents --json` (Tier B liveness, PWT-DS1-LIVE — the flag is deliberately NOT auto-deleted so Tier B can read it beyond the window). Backstop for the visual systemMessage marker check in manifest Step 3a. |
| `.claude/state/plan-w-team-directive-<hash>.txt` | `plan-w-team-directive-` | - | audit-trail | Directive overflow file written by `pwt-goal.sh` when the wrapped /goal text exceeds the overflow threshold (default 3000 chars). Filename hash = first 12 hex chars of `sha256(REQUEST)`. Idempotent: same REQUEST → same hash → file reused. The worker's first action is to Read this file (no shell-code reader, hence `audit-trail` mode). Persists across runs as a durable audit trail. |
| `.claude/state/plan-w-team-spec-fanout-$SLUG.json` | `FANOUT=".claude/state/plan-w-team-spec-fanout` | `FANOUT_STATE=".claude/state/plan-w-team-spec-fanout` | audit-trail | Step-1 §1b-pre multi-angle spec fan-out advisory record (reviewers, verdicts, findings_folded/deferred). Writer: `01-specification.md` §1b-pre (AUTO since 1.50.0 — fires on non-trivial specs; `PLAN_W_TEAM_SPEC_FANOUT=0` opt-out / `=1` force). Reader: `07-retro.md` §8j-nonies (advisory keep/park score, never blocks). audit-trail: an auto-skipped (trivial-spec) or opted-out fan-out leaves no file and the reader no-ops. |
| `.claude/state/plan-w-team-project-version-baseline-$SLUG.json` | `plan-w-team-project-version-baseline-` | `PVBASE=".claude/state/plan-w-team-project-version-baseline` | handoff | PWT-PVER (2.5.0) consumer-version baseline. Writer: `03-execute.md` preflight (records `baseline_version` + `path` on the pristine base tree via `plan-w-team-project-version.sh detect`). Reader: `05-ship.md` §6d (`verify --baseline`). Fail-open: no artifact detected → no file, gate no-ops. |
| `.claude/state/plan-w-team-project-version-$SLUG.json` | `plan-w-team-project-version-\$SLUG` | - | audit-trail | PWT-PVER (2.5.0) consumer-version ship provenance (`project_version`, `recorded_at`, `spawn_path`). Writer: `05-ship.md` §6d after the verify/auto-bump. Mirrors the `skill_version` provenance for consumer feature ships; read by the completion summary. |
| `.claude/state/plan-w-team-test-baseline-$SLUG.json` | `BASELINE_FILE="\$STATE_DIR/plan-w-team-test-baseline-` | `BASELINE_FILE="\$STATE_DIR/plan-w-team-test-baseline-` | handoff | PWT-REGRESS (2.5.0) no-regression baseline. Writer: `plan-w-team-regression-gate.sh baseline` (run at `03-execute.md` preflight on the base tree). Reader: same script's `verify` at `05-ship.md` §6b-regress. Records `green`, `failing[]`, `pass_count`, `fidelity`; `{"no_suite":true}` in repos without a runnable suite (verify then skips). |
| `.claude/state/plan-w-team-regression-waiver-$SLUG` | `plan-w-team-regression-waiver-` | `WAIVER_FILE="\$STATE_DIR/plan-w-team-regression-waiver-` | handoff | PWT-REGRESS (2.5.0) waiver. User-created escape hatch authorizing an INTENTIONAL test removal/rename. Reader: `plan-w-team-regression-gate.sh verify` — a present waiver → exit 0 (not blocking). Never auto-created. |
| `.claude/state/plan-w-team-sync-confirm-$SLUG.json` | `plan-w-team-sync-confirm-` | - | audit-trail | PWT-SHIPSYNC (2.5.0) same-machine fleet-propagation confirmation. Writer: `05-ship.md` §6h-bis after a successful skill-self-ship fleet sync. Records `synced_at`, `scope`. Absent on consumer ships (distributed via origin) or when `PLAN_W_TEAM_DISABLE_SHIP_SYNC=1`. |
| `.claude/state/plan-w-team-deep-audit-$SLUG.jsonl` | `plan-w-team-deep-audit-` | `DA_AUDIT=".claude/state/plan-w-team-deep-audit` | audit-trail | C4 pilot: opt-in Tier-1 deep-audit Agent()-fan-out breadth findings (one JSON line per finding). Writer: `shared/deep-audit.md` (gated `PLAN_W_TEAM_DEEP_AUDIT=1`, default OFF). Reader: `07-retro.md` §8e "Deep-Audit Cost" (advisory cost/value score, never blocks). audit-trail: an unrun (default-OFF) sweep leaves no file and the reader no-ops. |
| `.claude/state/plan-w-team-completion-$SLUG.json` | `JSON_OUT="\$\{STATE_DIR\}/plan-w-team-completion-` | - | audit-trail | Run completion summary (machine-readable). Writer: `.claude/scripts/plan-w-team-completion-summary.sh` invoked from `07-retro.md` §8k. Schema: `slug`, `generated_at`, `writer_version`, `spec_path`, `ac_snapshot_path`, `transcript_path`, `duration`, `ac_rollup{declared,passed,failed,missing,holistic_check}`, `stages`, `git{base_commit,head_commit,commits,files_changed,insertions,deletions,diff_stat_summary}`, `tests{added_or_modified_files,files}`, `ship{verdict,evidence_line}`. No in-code reader: terminal archival output for downstream tooling (dashboards, audit scripts). Retention: keep 30 days then archive to `docs/operations/run-completions/<date>-<slug>/`. Schema versioned via `writer_version` field; `HOLISTIC_CHECK_*` anchor literals are stable across writer versions (see `docs/specs/reporting-holistic-check.md`). |
| `.claude/state/plan-w-team-completion-$SLUG.md` | `MD_OUT="\$\{STATE_DIR\}/plan-w-team-completion-` | - | audit-trail | Run completion summary (human-readable). Companion to the json artifact. Contains a `## Spec Compliance Check` section with one of the four literal anchors: `HOLISTIC_CHECK_PASS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | FAIL | SKIPPED | UNKNOWN`(grep-able by downstream audit tooling). Same retention policy as the json. Writer:`.claude/scripts/plan-w-team-completion-summary.sh`. Spec: `docs/specs/retro-completion-summary.md`. Holistic-check contract: `docs/specs/reporting-holistic-check.md`. |
| `.claude/state/plan-w-team-manifest-$SLUG.json` | `plan-w-team-manifest-` | `plan-w-team-manifest-` | handoff | Canonical run manifest. Writer: `pwt-manifest.sh`; reader: `pwt-status.sh` rollup. Fail-OPEN observability (never a gate). Registered in the P11 hardening (audit). |
| `.claude/state/plan-w-team-stage-events-$SLUG.jsonl` | `plan-w-team-stage-events-` | - | audit-trail | Per-stage event stream. Writer: `pwt-manifest.sh`. No in-code reader (the manifest is the rollup surface); terminal audit trail. |
| `.claude/state/plan-w-team-empty-ship-attempts-$SLUG.txt` | `plan-w-team-empty-ship-attempts-` | - | audit-trail | Diagnostic empty-ship attempt counter. Writer: `plan-w-team-empty-ship-guard.sh` (§6g loop-breaker) — increments on each positively-confirmed empty ship (0 commits ahead of base + clean tracked tree) so a recurring empty-worktree-at-ship loop is visible in state. Best-effort, never affects the guard verdict; no in-code reader (terminal diagnostic). |
| `.claude/state/plan-w-team-fable-ledger-$SLUG.jsonl` | `plan-w-team-fable-ledger-` | - | audit-trail | Model Tiering v3 Fable spend ledger — one JSONL row per guard invocation (ALLOW and SKIP alike), schema `{ts,slug,kind,verdict,reason,bucket,task,note}`. Writer AND reader: `.claude/scripts/plan-w-team-fable-guard.sh` (it counts its own `verdict=="ALLOW" and kind=="escalation"` rows to enforce the per-run cap, so the ledger IS the counter — no second source that can disagree). Registered `audit-trail`, NOT `handoff`, deliberately: the self-read satisfies `plan-w-team-symmetry-check.sh` only via the non-markdown-reader exemption (`:152-166`), and claiming `handoff` on that basis would be the very "write-only by accident" defect this registry exists to catch. Rows are emitted with `jq -cn --arg`, never printf interpolation — a malformed row is a cap bypass, not just bad logging. Resolved to the MAIN checkout via `git rev-parse --git-common-dir` so a worktree cannot fork the counter. Removed on RETRO_SUCCESS with the other per-run artifacts. |
| `.claude/state/plan-w-team-fable-ledger-$SLUG.lock` | `LOCK_DIR="\$STATE_DIR/plan-w-team-fable-ledger-` | `mkdir "\$LOCK_DIR"` | enforcing | Atomic-append `mkdir` lock serializing the guard's read-count-append sequence. Without it two concurrent escalations can each read `used=1` and both proceed, granting 3 against a cap of 2. Writer+reader: `plan-w-team-fable-guard.sh` (released on EXIT/INT/TERM). Same idiom as `plan-w-team-push.lock` / `plan-w-team-test-green.lock` (macOS has no `flock`). |
| `.claude/state/plan-w-team-ship-verdict-$SLUG.json` | `plan-w-team-ship-verdict` | `plan-w-team-ship-verdict` | handoff | C3 deterministic ship verdict. Writer: `05-ship.md` End-of-Stage block (only after every §6 ENFORCING gate passes); reader: goal-evaluator worker-mode SUCCESS corroboration. |
| `.claude/state/plan-w-team-landed-$SLUG.json` | `plan-w-team-landed-` | `plan-w-team-landed-` | handoff | F6 deterministic landing verdict `{schema, slug, verdict, reason, sha, landed_sha, branch, default_branch, default_ref, remote, merged, merged_via, tags, tag_reachable, pushed, commits_ahead, verified_at}`. Writer: `.claude/scripts/plan-w-team-land.sh verify` ONLY, and only after recomputing merged / tag-reachable / pushed from git — there is no `--force` and no caller-asserted path, because the artifact IS the gate. Dual-written to the worktree + MAIN state dirs (same set as the goal-state's dual-seed) since the evaluator and the watcher read from different sides. Readers: `plan-w-team-goal-evaluator.sh` F6 AND-check (fail-CLOSED — absent ⇒ SUCCESS withheld for worktree-isolated runs), `plan-w-team-await-terminal.sh` (`__landing_missing`, gating the TERTIARY transcript detector), `plan-w-team-surface-status.sh` (`landed` field + the grep-able `landed=<sha>` line). Removed by `verify` itself when the predicate turns false, so a rewound landing cannot keep passing. Kill switch on the gate (not the artifact): `PWT_DISABLE_LANDING_GATE=1`. |
| `.claude/state/plan-w-team-land-audit.jsonl` | `plan-w-team-land-audit` | - | audit-trail | Landing / remediation audit stream, `{ts, slug, action, verdict, sha, landed_sha, detail}` — one row per `verify`, per `resume`, and per REFUSED resume (already-landed / already-terminal / no-worker-sid / no-steer-bin), so a refusal is as diagnosable as an action. Writer: `plan-w-team-land.sh`. Best-effort: an audit write failure never changes a verdict. No in-code reader (terminal audit trail). |
| `.claude/state/plan-w-team-test-green-$SLUG.json` | `plan-w-team-test-green` | `plan-w-team-test-green` | handoff | T1/R2 deterministic skill-suite verdict `{ts, slug, suite_exit, green, reason, log_path, duration_s}`. Green ONLY on run.sh's literal trailing `SUITE_EXIT=0`; marker-absent (killed/truncated suite) is RED by construction. Writer: `plan-w-team-test-green.sh` (dual-written to the worktree + main state dirs, same set as `__pwt_emit_goal_state`, temp+rename). Readers: `05-ship.md` §6b CONDITIONAL gate (ENFORCING when the harness exists), `plan-w-team-goal-evaluator.sh` SUCCESS corroboration (fail-OPEN on absent/stale/malformed), `--check` (supervisor Standard Tick, advisory). Companion `.log` beside it. Absent in repos without the skill harness (wrapper exits 3 NO-SUITE, writes nothing) — every reader no-ops. |
| `.claude/state/plan-w-team-test-green.lock` | `LOCK_NAME="\$\{STATE_FILE_PREFIX\}\.lock"` | `LOCK_DIR="\$STATE_DIR/\$LOCK_NAME"` | enforcing | Per-CHECKOUT (not per-slug) mkdir lock serializing skill-suite runs — the suite mutates shared per-checkout resources (`tests/skill/results/latest.json`, the bats tmp tree, the R6 state-leak bracket), so concurrent runs corrupt each other's verdict regardless of slug. Writer+reader: `plan-w-team-test-green.sh` (busy → exit 4; released on EXIT/INT/TERM). Same idiom as `plan-w-team-push.lock`. |
| `.claude/state/plan-w-team-content-signal-suspects-$SLUG.txt` | `plan-w-team-content-signal-suspects` | `plan-w-team-content-signal-suspects` | handoff | P9c access-control content-scan suspects. Writer: `access-control-content-scan.sh` + `05-ship.md`; reader: `05-ship.md` §6c-ter GIGO backstop / `04-fix-first-review.md` §5b floor. |
| `.claude/state/plan-w-team-bypass-$SLUG.log` | `plan-w-team-bypass-` | `plan-w-team-bypass-` | handoff | P1 stage-file-bypass marker log. Writer: lead append (plan-w-team.md Fast Path); reader: `plan-w-team-bypass-rate.sh` → retro §8j-octies. |
| `.claude/state/plan-w-team-retro-capture-$SLUG.json` | `plan-w-team-retro-capture\.sh` | `CAPTURE=".claude/state/plan-w-team-retro-capture` | handoff | Recursive-improvement capture record (Deliverable 3, retro §8j-decies). Writer: `plan-w-team-retro-capture.sh`; reader: `07-retro.md` §8j-decies persists it into the retro JSON `quality_signals.recursive_capture`. Per-run; removed on RETRO_SUCCESS. |
| `.claude/state/plan-w-team-retro-capture-history.jsonl` | `plan-w-team-retro-capture-history` | `plan-w-team-retro-capture-history` | handoff | Cross-run rolling history of recursive-capture records (best-practice adherence regression baseline). Writer + reader: `plan-w-team-retro-capture.sh` (appends each run; reads the last entry to detect regressions). Durable across runs — NOT deleted at retro. |
| `.claude/state/plan-w-team-recursive-followups.jsonl` | `plan-w-team-recursive-followups` | `FOLLOWUPS_SH=".claude/scripts/plan-w-team-followups.sh"` | handoff | Durable cross-run queue of recursive-improvement follow-ups (Deliverable 3). Writers: `plan-w-team-retro-capture.sh` (one `open` row per follow-up), `plan-w-team-followups.sh add` (one `open` row, queued at the moment a Step-5 deferral is decided — `04-fix-first-review.md` §5d outcome 3 / §5c-quinquies), and `plan-w-team-followups.sh close` (appends a **resolution row**); readers: `plan-w-team.md` Pre-Flight carry-forward surfacing (which **delegates** to `plan-w-team-followups.sh --json stats` rather than reading the ledger itself — hence the `FOLLOWUPS_SH=` reader pattern), `plan-w-team-followups.sh list/stats`, `plan-w-team-followup-drain.sh`. Durable across runs — NOT deleted at retro. **APPEND-ONLY, so a row's own `status` is NOT its effective status:** closure never rewrites the original, which keeps `status:"open"` forever. A row is closed iff a LATER row carries `closes_index: <its 0-based raw index>`. Any new reader MUST resolve through that back-reference — reading `.status` directly is why closures were inert until 2026-08-02 (1.65.1) and the backlog could not drain. Count over rows WITHOUT `closes_index` (resolution rows are bookkeeping, not backlog) and de-dup by distinct closed index (pre-1.65.1 ledgers carry duplicate closures). |
| `.claude/state/plan-w-team-fleet-intent-$SLUG.jsonl` | `plan-w-team-fleet-intent-` | `plan-w-team-fleet-intent-` | handoff | Agent_id↔task_id intent sidecar (lead → fleet). Writer: lead; readers: `plan-w-team-fleet-query.sh`, `07-retro.md`. Registered in the round-2 P11 Pass-2 fix. |
| `.claude/state/plan-w-team-skill-version-$SLUG.json` | `plan-w-team-skill-version-` | `plan-w-team-skill-version-` | handoff | Skill-version spawn sidecar (all spawn paths). Writer: `pwt-goal.sh`; reader: `plan-w-team-completion-summary.sh`. Schema in `shared/versioning.md`. |
| `.claude/state/plan-w-team-pass1-synthesis-$SLUG.md` | `plan-w-team-pass1-synthesis-` | - | audit-trail | Step 5 §5b-pre Pass-1 fan-out synthesis record (synthesized CRITICAL list / skip reason). Writer: `04-fix-first-review.md`. Terminal audit record (retro may audit). |
| `.claude/state/plan-w-team-test-output-$SLUG.log` | `plan-w-team-test-output-` | - | audit-trail | Step-6 test-suite output capture for retro metrics. Writer: `plan-w-team-retro-metrics.sh`. Terminal audit record. |
| `.claude/state/plan-w-team-docs-waived-$SLUG.txt` | `WAIVE_FILE=".claude/state/plan-w-team-docs-waived` | `WAIVE_FILE` | handoff | A1/A6/C2 net-new-surface documentation waiver list (user/lead-curated). One path-or-token per line; `*` waives all. Writer/reader: `plan-w-team-netnew-surface.sh` (constructs the default path from `--slug`, then loads tokens); also referenced by `06-post-ship.md` §7a-bis. Suppresses an `UNDOCUMENTED` net-new item from the §7f gate. See `docs/operations/pwt-doc-secret-handling.md`. |
| `.claude/state/plan-w-team-credwall-$SLUG.json` | `plan-w-team-credwall-` | `plan-w-team-credwall-` | handoff | Credential-wall escalation artifact (brief credential-wall-escalation). Writer: `plan-w-team-credential-wall-detect.sh` hook (PostToolUse/PostToolUseFailure) — persists the EXACT missing secret name + documented operator action + `resolved:false`, survives compaction. Reader: `plan-w-team-credential-wall-gate.sh` (ENFORCING ship gate §6a-quinquies, fails closed while unresolved). Contains a secret NAME only, never a value. See `shared/secret-safety.md §REQ-6`. |
| `.claude/state/plan-w-team-completion-$SLUG.html` | `HTML_OUT=` | - | audit-trail | Self-contained-HTML run report (visual-artifacts Hook 1). Writer: `plan-w-team-completion-summary.sh` (gated `PWT_EMIT_HTML_REPORT=1`, default OFF) via the `plan-w-team-render-artifact.sh` substrate, from the EXISTING completion JSON. Human-consumed (opened in a browser) — no code reader, hence `audit-trail`. Gitignored (`plan-w-team-completion-*.html`) so it never leaks into a sync commit. An unset toggle leaves no file. See `docs/operations/plan-w-team-visual-artifacts.md`. |
| `.claude/state/plan-w-team-status-<date>.md` | `plan-w-team-status-` | - | audit-trail | Run-State Router Status/Readiness-mode gap report (Item 3). Writer: `plan-w-team.md` §"Status / Readiness Mode" (lead-authored: current state + remaining gaps + a recommended next run), one dated file per invocation (`<date>` = `YYYY-MM-DD`). Read-only mode — no code reader (human-consumed), hence `audit-trail`. The mode performs zero writes outside this artifact. See `docs/specs/run-state-router.md`. |
| `.claude/state/plan-w-team-run-state-audit.jsonl` | `plan-w-team-run-state-audit` | `plan-w-team-run-state-audit` | audit-trail | Run-State Router Step -1 always-on evidence (1.54.0). Writer: lead append per manifest Step -1 (one JSON line `{ts, topic, verdict, slug, route}` on EVERY invocation — including `no-prior`, so "router ran and said no-prior" is provable post-hoc; the detector script itself stays read-only per its AC2 tree-hash contract). NOT deleted at retro (unlike the per-slug bypass log) — cross-run audit trail. No code reader (human/field-audit consumed, hence `audit-trail`); retro §8j-octies reads only the per-slug bypass log. |
| `.claude/state/plan-w-team-abstraction-claims-$SLUG.jsonl` | `LEDGER="\$STATE_DIR/plan-w-team-abstraction-claims-` | `-f "\$LEDGER"` | audit-trail | Nascent-shared-abstraction claim ledger (recursive-followup row 4). Writer AND reader: `.claude/scripts/plan-w-team-claim-abstraction.sh` — `claim`/`release` append `granted`/`released`/`conflict` rows, `verify` (Step 5 §5c-quater, the diff-driven Layer-1 gate) reads them to grade duplicate-finding severity. Registered `audit-trail`, NOT `handoff`, deliberately: same rationale as the Fable ledger row above — writer and reader are the same script, and claiming `handoff` on a self-read is precisely the "write-only by accident" defect this registry exists to catch. Rows are `jq -cn --arg`-built, never printf interpolation. Resolved to the MAIN checkout via `git rev-parse --git-common-dir` so a worktree cannot fork the ledger. Removed on `RETRO_SUCCESS` (`07-retro.md`) from the resolved MAIN root. |
| `.claude/state/plan-w-team-abstraction-claims-$SLUG.degraded` | `DEGRADED="\$STATE_DIR/plan-w-team-abstraction-claims-` | `-f "\$DEGRADED"` | audit-trail | Claim-lock degraded sentinel. Every fail-open path in `plan-w-team-claim-abstraction.sh` (unresolvable root, unwritable ledger, row cap, malformed row) appends a line here; `verify` reads it and prints a `DEGRADED: …` notice so a wedged claim layer cannot masquerade as a clean run. Writer AND reader: same script, same audit-trail rationale as the ledger row above. Removed on `RETRO_SUCCESS` from the resolved MAIN root. |
| `.claude/state/plan-w-team-lane-release-$SLUG.json` | `plan-w-team-lane-release-` | `plan-w-team-lane-release-` | handoff | PWT-LANE1 lane-release valve `{released_by, reason}`. Writer: the USER, out-of-band (a fresh unbound session or a bare terminal — DELIBERATELY no pipeline writer, and the lane-guard denies the bound supervisor writing it, or the guard would be self-releasing). Readers: `plan-w-team-lane-guard.sh` + `plan-w-team-lane-context.sh` (presence = lane released; guard stands down, role contract stops emitting). See `docs/operations/lane-enforcement.md`. |
| `.claude/state/plan-w-team-lane-guard-audit.jsonl` | `plan-w-team-lane-guard-audit` | - | audit-trail | Lane-guard verdict audit `{ts, sid, tool, decision, kind, slug, target, reason}`. Writer: `plan-w-team-lane-guard.sh` (`jq -cn --arg`, best-effort). **Since F4 (2026-08-19) it records ALLOWs as well as DENYs** — the host-hygiene ALLOW class (`kind: host-hygiene-process` / `host-hygiene-mutation`), the classifier exemptions (`kind: classifier-exemption`), and **since 2.15.0 the resolved-target out-of-repo ALLOWs** (`kind: outside-repo-git-write` / `outside-repo-edit`) — because a guard that permits something without recording it leaves the next incident as blind as the last. **Deliberately NOT logged: the "no live lane applies" path** — that is not a verdict, and a row per Bash call in every session on the machine would be a disk-exhaustion vector rather than an audit trail (recorded in the F4 threat model). No code reader — human/retro forensic record. Durable across runs. |
| `.claude/state/plan-w-team-lane-guard-allow-<sid8>.json` | `plan-w-team-lane-guard-allow-` | `plan-w-team-lane-guard-allow-` | handoff | PWT-LANE1 operator allowance valve `{until, scope}` (2.15.0). Writer: the OPERATOR, out-of-band (a fresh session or a bare terminal — DELIBERATELY no pipeline writer, exactly like the lane-release valve). Reader: `plan-w-team-lane-guard.sh` (`__outside_repo_allowance_active`). Relaxes ONLY the fail-closed UNRESOLVABLE branch of the git-write/in-place classes for the named sid, and ONLY while `scope=="outside-repo"` and `until` is in the future — it NEVER allows a provably-inside target. Keyed by the bound session's 8-hex sid so it cannot be forged for another session. See `docs/operations/lane-enforcement.md`. |
| `.claude/state/plan-w-team-ds1-audit.jsonl` | `plan-w-team-ds1-audit` | - | audit-trail | PWT-DS1 double-spawn-guard verdict audit `{ts, decision, tier, worker_sid, reason, caller_pid}` — one row per guard evaluation, refuse AND proceed alike. Writer: `pwt-goal.sh` §F3 (`--worker-only` and `--launch`, fail-open `printf`). No code reader — human/forensic. **Registered 2026-08-19**: it shipped with the 2.10.0 double-spawn trifecta fix but was never added here, so `plan-w-team-symmetry-check.sh` reported it as an orphan reader (exit 4) — the exact "writer pointing at a wholly undeclared file" case this registry exists to catch. Durable across runs (NOT deleted at retro). |
| `.claude/state/plan-w-team-stall-events-$SLUG.jsonl` | `plan-w-team-stall-events-` | - | audit-trail | F2 verification-stall event stream — one row per bound-firing `{ts, slug, step, attempt, timeout_s, elapsed_s, pgid, orphans_after, action}`. Writer: `plan-w-team-verify-run.sh`. `orphans_after` is the zero-surviving-descendants contract MEASURED rather than assumed. No code reader (the retro/human reads it); this is the artifact whose absence made the 2026-08-19 run's 4.5-hour retry loop invisible. Removed on RETRO_SUCCESS with the other per-run artifacts. |
| `.claude/state/plan-w-team-stall-$SLUG.json` | `STALL_STATE="\$STATE_DIR/plan-w-team-stall-` | - | audit-trail | F2 consecutive-timeout counter `{slug, updated_at, steps:{<step>:{consecutive_timeouts, total_timeouts, last_ts}}}` — the state the K=3 retry cap is enforced from. Writer AND reader: `plan-w-team-verify-run.sh` (it counts its own rows, so the file IS the counter — no second source that can disagree). Registered `audit-trail`, NOT `handoff`, deliberately: the self-read satisfies `plan-w-team-symmetry-check.sh` only via the non-markdown-reader exemption, and claiming `handoff` on that basis would be the very "write-only by accident" defect this registry exists to catch (same reasoning as `plan-w-team-fable-ledger-$SLUG.jsonl`). Guarded by a `mkdir` lock so two runners cannot both read "2" and grant a 4th attempt. |
| `.claude/state/plan-w-team-host-distress-$SLUG.json` | `plan-w-team-host-distress-` | - | audit-trail | F2/F3 host-distress diagnosis `{ts, slug, source, host_health, choices, guidance}`. TWO writers, distinguished by `source`: `plan-w-team-verify-run.sh` (`source: "verify-run"`) writes it when the K=3 retry cap is reached, carrying the mandatory diagnosis and the two legal continuations (`degraded-mode` / `user-escalation-halt`); `plan-w-team-await-terminal.sh` (`source: "await-terminal"`) writes/updates it when the supervisor confirms distress on two consecutive samples. No code reader — it is consumed by the lead following `03-execute.md` §Long Verifications and by the operator reading the `⚠ HOST-DISTRESS` block. Removed on RETRO_SUCCESS. |
| `.claude/state/plan-w-team-liveness-$SLUG.jsonl` | `plan-w-team-liveness-` | - | audit-trail | Governor Contract phase 1 (C3, §4.5) GOVERNED-ONLY liveness-consult record. Writer: `pwt-lane-alive.sh` — when governed AND the manifest carries a `liveness_cmd`, it records BOTH answers `{ts, slug, process, registry, governor, verdict}` per consult and appends a `{kind:"liveness-disagreement", …, resolution:"prefer-process"}` row on disagreement. **UNGOVERNED this file is NEVER created** (parity — asserted by `pwt-lane-alive.bats` + `governor-parity.bats`). No in-code reader in phase 1 — it is the phase-2 C1 event-sink's input; human/forensic until then. Removed on RETRO_SUCCESS. |
| `.claude/state/pwt-governor.json` | `-` | `pwt-governor.json` | audit-trail | Governor Contract phase 1 (§2) EXTERNAL-INPUT manifest (schema `pwt-governor/1`). **No pipeline writer** — operator-authored out-of-band, exactly like `plan-w-team-lane-release-<slug>.json`; **gitignored** so a branch author cannot commit it. Reader: `pwt-governor-lib.sh` (`pwt_governed` / `pwt_governor_get`). Detection is OFF by default; a broken manifest is treated as ABSENT plus one stderr warning. Not per-run (not removed at retro). NOTE: `pwt-*`-prefixed, so outside `plan-w-team-symmetry-check.sh`'s `plan-w-team-*` scan — registered here for documentation, not because the checker requires it. |
| `.claude/state/pwt-lane-alive-memo-$SLUG.json` | `pwt-lane-alive-memo-` | `pwt-lane-alive-memo-` | audit-trail | Governor Contract phase 1 (C3, §4.4) lane-guard liveness memo `{goal_mtime, ts, verdict}`. Writer AND reader: `plan-w-team-lane-guard.sh` (`__lg_lane_alive_verdict`) — the guard runs on every Bash call, so the confirmed-dead-release consult is memoized per goal-state mtime + `PWT_LANE_ALIVE_MEMO_TTL_S` (default 30s). Best-effort/advisory (a torn write just forces a recompute). `pwt-*`-prefixed → outside the symmetry checker's `plan-w-team-*` scan; registered for documentation. |
| `.claude/state/plan-w-team-approver-audit-$SLUG.jsonl` | `plan-w-team-approver-audit-` | - | audit-trail | Governor Contract phase 2 (C4) delegated-approver decision audit `{ts, slug, gate, decision, by, grant_ref, outcome, reason}` — one row per consumed decision (approved/denied/escalated/halted). Writer: `pwt-approver.sh` (`jq -cn --arg`, best-effort). No code reader — human/forensic record of every governor decision, carrying `by` and `grant_ref` for accountability. Written into the run's operator state dir; not per-run-slug-cleaned at retro by design (accountability trail). |
| `<approver.dir>/<slug>.<gate>.request.json` | `\.request\.json` (in `pwt-approver.sh`) | - | audit-trail | Governor Contract phase 2 (C4) pause request (schema `pwt-pause/1`: `{schema,slug,gate,since,evidence,proposed_action,policy_refs,risk}`). Writer: `pwt-approver.sh await` (the RUN). Reader: the out-of-band governor + `pwt-status.sh` (for `pause_site`). `<approver.dir>` is governor-configured (default `.claude/state/pwt-pause/`); NOT `plan-w-team-*` prefixed → documentation-only (outside the symmetry scan). GOVERNED-ONLY — never created ungoverned. |
| `<approver.dir>/<slug>.<gate>.decision.json` | `-` | `\.decision\.json` (in `pwt-approver.sh`) | handoff | Governor Contract phase 2 (C4) EXTERNAL-INPUT governor decision `{decision,by,reason,ts,grant_ref}`. **No pipeline writer** — authored out-of-band by the GOVERNOR, exactly like `plan-w-team-lane-release-<slug>.json`; the lane guard denies BOTH the worker and the bound supervisor writing it (structural self-approval prevention). Reader: `pwt-approver.sh consume`. `<approver.dir>` is governor-configured, refused if git-tracked or escaping the repo; NOT `plan-w-team-*` prefixed → documentation-only. GOVERNED-ONLY. |
| `.claude/state/plan-w-team-given-spec-$SLUG.json` | `plan-w-team-given-spec-` | - | audit-trail | Governor Contract phase 3 (C6) given-spec provenance sidecar `{slug, source, governor, freshness_gate, installed, validated_at}`. Writer: `pwt-given-spec.sh` (`jq -cn --arg`, best-effort) after it validates + installs a governor-supplied spec at `docs/specs/<slug>.md`. `freshness_gate` is the TRUTHFUL outcome `pass`/`skipped:binary-absent`/`disabled:killswitch` — never a false `pass`. No code reader — human/governor forensic provenance record (the human-readable provenance also lives IN the spec artifact). Written to the checkout `pwt-given-spec.sh` runs in; the sidecar surfaces the supply provenance to the Step-5 reviewer. Not per-run-cleaned by design (provenance trail). GOVERNED context but written whenever `--spec` is passed (an operator option too). |

## Claude Code interactions (2.1.139+)

- **`claude project purge [path]`** removes all `.claude/state/plan-w-team-*` artifacts including in-flight baselines, scope-locks, AC snapshots, and retros. Treat purge mid-feature as equivalent to abandoning the SLUG — the workflow lock dir is removed but the workflow itself cannot detect this. If a user purges mid-run, instruct them to start a new SLUG rather than resume.
- **`worktree.baseRef` inheritance** — Builder worktrees spawned in Step 3-4 inherit the repo's `worktree.baseRef` setting. Default changed to `"fresh"` in Claude Code 2.1.133, which means builders branch from `origin/<default>` and CANNOT see the lead's local-only spec commit. Step 3-4 has a pre-fan-out guard that detects this and pushes the spec before spawning. If the repo has no remote, set `worktree.baseRef: "head"` in `.claude/settings.json` to restore pre-2.1.133 behavior.

## When adding a new state artifact

1. Add a row to the table above **in the same commit** as the writer code.
2. Run `.claude/scripts/plan-w-team-symmetry-check.sh` locally — it must exit 0.
3. If the artifact is write-only (audit trail), set `mode: audit-trail` and put a dash (`-`) in `reader_grep`.
4. If the checker flags a false positive, scope the grep patterns tighter (quote the variable assignment) rather than relaxing the mode.

## Checker invocation

```bash
# snippet-lint: skip — illustrative `<path>` placeholder, not executable as-is
.claude/scripts/plan-w-team-symmetry-check.sh                     # exit 0 = pass
.claude/scripts/plan-w-team-symmetry-check.sh --json               # machine-readable
.claude/scripts/plan-w-team-symmetry-check.sh --registry <path>    # alt registry (testing)
```

Exit codes:

- `0` — symmetric
- `1` — enforcing orphan (registry entry with no reader grep match)
- `2` — stale registry entry (no writer grep match — likely renamed or removed)
- `3` — environment failure (ripgrep missing, registry malformed)
- `4` — orphan reader (code references `.claude/state/plan-w-team-*` with no matching registry entry)

## Where this runs

- **Step 5 review** — fail-closed for exit 1 or 2; advisory for audit-trail-only warnings.
- **Step 8 retro** — score 5 (pass) / 3 (audit-trail warnings only) / 1 (enforcing fail in Step 5). Score <4 feeds §8i friction log with category `spec-gap`.
